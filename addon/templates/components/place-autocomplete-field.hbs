{{#if hasBlock}}
  {{yield}}
{{else}}
  {{input value=value
          classBinding="inputClass"
          tabindex=tabindex
          disabled=disabled
          placeholder=placeholder
          focus-out='focusOut'
          bubbles=true}}
{{/if}}